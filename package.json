{
  "name": "mcp-study",
  "version": "1.0.0",
  "description": "Learning Model Context Protocol (MCP)",
  "private": true,
  "workspaces": {
    "packages": [
      "packages/*"
    ]
  },
  "scripts": {
    "build": "pnpm -r build",
    "start:filesystem-client": "pnpm --filter filesystem-client start",
    "start:aws-mcp": "pnpm --filter aws-mcp start",
    "dev:aws-mcp": "pnpm --filter aws-mcp dev"
  },
  "devDependencies": {
    "@biomejs/biome": "1.9.4",
    "@types/node": "^22.13.14",
    "typescript": "^5.8.2"
  }
}
